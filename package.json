{
  "name": "wepublish-faas",
  "version": "1.0.0",
  "private": true,
  "description": "We.Publish FaaS monorepo — Directus backend (apps/directus) + Nuxt frontend (apps/front). Not an npm workspace: each app installs and builds independently. This root package owns only the shared pre-commit tooling (husky + lint-staged + prettier).",
  "scripts": {
    "prepare": "husky",
    "lint": "prettier . --write --ignore-unknown"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "3.8.1"
  }
}
