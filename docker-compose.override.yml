# docker-compose.override.yml
# Local dev only. Dokploy reads only docker-compose.yml, so it ignores this.
services:
  directus:
    ports:
      - '${DIRECTUS_PORT:-8055}:8055'
  front:
    ports:
      - '${FRONT_PORT:-3000}:3000'
