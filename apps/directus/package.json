{
  "name": "cms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "<22.0.0"
  },
  "scripts": {
    "dev": "docker compose up -d && npm run directus:start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "schema:dump": "directus-sync pull",
    "schema:load": "directus-sync push",
    "database:migrate": "npx directus database migrate:latest",
    "directus:init": "ADMIN_EMAIL=admin@wepublish.ch ADMIN_PASSWORD=admin123 PUBLIC_URL=/ DB_HOST=127.0.0.1 DB_PASSWORD=password KEY=59ccea19-23112-4a08-8sw72-dd2312341412 SECRET=1234 npx directus bootstrap && npx directus database migrate:latest && npm run schema:load",
    "directus:start": "CACHE_ENABLED=false EXTENSIONS_AUTO_RELOAD=true PUBLIC_URL=/ DB_HOST=127.0.0.1 DB_PASSWORD=password KEY=59ccea19-23112-4a08-8sw72-dd2312341412 SECRET=1234 npx directus start",
    "build:extensions": "for d in extensions/*; do [ -f \"$d/package.json\" ] && (cd \"$d\" && npm ci && npm run build); done",
    "setup": "cp .env.example .env && npm install && npm run schema:seed",
    "db:start": "docker-compose up",
    "db:reset": "docker-compose down",
    "lint": "prettier . --write --ignore-unknown"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@directus/errors": "^2.1.0",
    "bexio": "^3.5.0",
    "directus": "11.13.4",
    "directus-extension-sync": "^3.0.5",
    "directus-sync": "^3.4.1"
  },
  "devDependencies": {
    "prettier": "3.8.1"
  }
}
